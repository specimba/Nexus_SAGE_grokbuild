---
name: sage-cycle-compiler
description: Compile the SAGE edition from free X/HF ingest. Do not use X Ads or Missed-DNA as news. No lock theater.
---

# sage-cycle-compiler

1. INGEST: Hugging Face daily_papers live. X watchlist snapshot (no paid API). Gmail newsletter highlights. Log each source: ok, count, timestamp, live vs snapshot.
2. RANK: confirmed primary > rumor; incident > product; keep ≠ upvote. `rankClaim()` / `keepPaper()` run on compile.
3. COMPILE `fetchEdition()` → `compileDigest()` — one lead, updates, related, also, rumors, papers.
4. WRITE the briefing in English. No cycle seals, no Sol, no unlock probes, no filing codes in the UI.
5. VERIFY the edition has a lead, an ingest log, live papers, and a 90-second skim.
